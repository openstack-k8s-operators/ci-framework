groups:
  - name: openstack-operator
    tests:
      - tempest.api.identity.*.v3
    releases:
      - all
  - name: cinder-operator
    tests:
      - tempest.api.volume
    releases:
      - all
  - name: keystone-operator
    tests:
      - tempest.api.identity.*.v3
    releases:
      - all
