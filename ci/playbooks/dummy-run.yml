---
- name: "Run ci/playbooks/edpm/run.yml"
  hosts: "{{ cifmw_zuul_target_host | default('all') }}"
  gather_facts: true
  tasks:
    - name: Debug failure
      ansible.builtin.assert:
        that: false
