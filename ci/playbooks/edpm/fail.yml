---
- name: "Run ci/playbooks/edpm/fail.yml"
  hosts: "{{ cifmw_zuul_target_host | default('all') }}"
  gather_facts: true
  tasks:
    - name: Fail
      ansible.builtin.fail:
        msg: "Debug failure"
