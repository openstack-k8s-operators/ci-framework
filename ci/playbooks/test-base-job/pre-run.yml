---
- name: "Run ci/playbooks/test-base-job/pre-run.yml"
  hosts: "{{ cifmw_target_host | default('localhost') }}"
  gather_facts: true
  tasks:
    - zuul_return:
        data:
          test_var: "test_var returned"
