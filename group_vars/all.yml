---
# This file contains all repeating variables, that can be set
# globaly instead of parse Zuul inventory file to get proper value.
#### GLOBAL VARS ####
cimfw_project_dir: "{{ zuul.projects['github.com/openstack-k8s-operators/ci-framework'].src_dir }}"
cimfw_project_dir_absolute: "{{ ansible_user_dir }}/{{ zuul.projects['github.com/openstack-k8s-operators/ci-framework'].src_dir }}"
