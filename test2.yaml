---
- name: First child
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Display another command for child
      ansible.builtin.command: |
        echo "First child"

    - name: First child play
      ansible.builtin.shell: |
        ansible-playbook test3.yaml
