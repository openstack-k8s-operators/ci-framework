---
- name: test
  hosts: localhost
  tasks:
    - name: Some example task
      ansible.builtin.command: |
        echo "some command"

    - name: Executing first nested
      ansible.builtin.shell: |
        ansible-playbook test2.yaml

- name: Some other test
  hosts: "{{ cifmw_target_host | default('localhost') }}"
  gather_facts: false
  tasks:
    - name: No nested
      ansible.builtin.shell: |
        echo "not nested"
