---
- name: Apply rabbitmq-ha-queues
  hosts: controller-0
  gather_facts: false
  tasks:
    - name: Workaround
      ansible.builtin.shell: |
        oc rsh -n openstack rabbitmq-cell1-server-0 rabbitmqctl set_policy ha-all "" '{"ha-mode":"exactly","ha-params":2,"ha-promote-on-shutdown":"always"}'
        oc rsh -n openstack rabbitmq-server-0 rabbitmqctl set_policy ha-all "" '{"ha-mode":"exactly","ha-params":2,"ha-promote-on-shutdown":"always"}'
