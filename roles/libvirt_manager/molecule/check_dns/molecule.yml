---
log: true

provisioner:
  name: ansible
  log: true
  inventory:
    host_vars:
      instance:
        cifmw_libvirt_manager_configuration_patch_01_more_computes:
          vms:
            compute:
              amount: 2
