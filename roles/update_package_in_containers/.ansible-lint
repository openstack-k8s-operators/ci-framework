---
# Ansible-lint configuration for update_package_in_containers role
skip_list:
  - no-changed-when  # We handle changed_when appropriately
  - command-instead-of-shell  # We need shell for piping
  - risky-shell-pipe  # We use set -o pipefail
warn_list:
  - experimental
