---
# Copyright Red Hat, Inc.
# All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.

# Fix dev-scripts bug where API_VIPS and INGRESS_VIPS use IPv4 DNS lookup even when IPv6 is configured
# This patches the set_api_and_ingress_vip function in network.sh to use AAAA records
# when IP_STACK is v6 or v6v4 (IPv6-only or IPv6-primary dual-stack deployments)
- name: Fix API_VIPS IPv6 DNS lookup in dev-scripts
  when:
    - "'manage_br_bridge' in cifmw_devscripts_config"
    - cifmw_devscripts_config['manage_br_bridge'] == 'n'
    - cifmw_devscripts_config.get('ip_stack', 'v4') == 'v6' or
      cifmw_devscripts_config.get('external_subnet_v6') is defined
  ansible.builtin.replace:
    path: "{{ cifmw_devscripts_repo_dir }}/network.sh"
    regexp: '^(\s+)API_VIPS=\$\(dig \+noall \+answer "api\.\$\{CLUSTER_DOMAIN\}"\s+\| awk ''\{print \$NF\}''\)$'
    replace: |2
            if [[ "${IP_STACK}" == "v6" || "${IP_STACK}" == "v6v4" ]]; then
                  API_VIPS=$(dig -t AAAA +noall +answer "api.${CLUSTER_DOMAIN}" | awk '{print $NF}')
              else
                  API_VIPS=$(dig +noall +answer "api.${CLUSTER_DOMAIN}" | awk '{print $NF}')
              fi
    owner: "{{ cifmw_devscripts_user }}"
    group: "{{ cifmw_devscripts_user }}"
    mode: "0644"

- name: Fix INGRESS_VIPS IPv6 DNS lookup in dev-scripts
  when:
    - "'manage_br_bridge' in cifmw_devscripts_config"
    - cifmw_devscripts_config['manage_br_bridge'] == 'n'
    - cifmw_devscripts_config.get('ip_stack', 'v4') == 'v6' or
      cifmw_devscripts_config.get('external_subnet_v6') is defined
  ansible.builtin.replace:
    path: "{{ cifmw_devscripts_repo_dir }}/network.sh"
    regexp: '^(\s+)INGRESS_VIPS=\$\(dig \+noall \+answer "test\.apps\.\$\{CLUSTER_DOMAIN\}"\s+\| awk ''\{print \$NF\}''\)$'
    replace: |2
            if [[ "${IP_STACK}" == "v6" || "${IP_STACK}" == "v6v4" ]]; then
                  INGRESS_VIPS=$(dig -t AAAA +noall +answer "test.apps.${CLUSTER_DOMAIN}" | awk '{print $NF}')
              else
                  INGRESS_VIPS=$(dig +noall +answer "test.apps.${CLUSTER_DOMAIN}" | awk '{print $NF}')
              fi
    owner: "{{ cifmw_devscripts_user }}"
    group: "{{ cifmw_devscripts_user }}"
    mode: "0644"
