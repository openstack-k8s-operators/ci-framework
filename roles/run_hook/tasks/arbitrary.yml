---
- name: Call code
  ansible.builtin.shell:  # noqa: command-instead-of-module
    cmd: "{{ hook.code }}"
  become: "{{ hook.escalate | default(false) }}"
  delegate_to: "{{ hook.delegate_to | default(omit) }}"
