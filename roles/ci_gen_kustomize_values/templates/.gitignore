# source: .gitignore
# dz-storage is a symlink to bgp-l3-xl (tracked in Git).
# If the symlink is removed and replaced by a real directory,
# ignore its contents to prevent accidental commits.
dz-storage/*
