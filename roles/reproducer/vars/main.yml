---
# Default repositories we always want to have
cifmw_reproducer_default_repositories:
  - src: "https://github.com/openstack-k8s-operators/ci-framework"
    dest: "{{ cifmw_reproducer_controller0_user_dir }}/src/github.com/openstack-k8s-operators/ci-framework"
  - src: "https://github.com/openstack-k8s-operators/install_yamls"
    dest: "{{ cifmw_reproducer_controller0_user_dir }}/src/github.com/openstack-k8s-operators/install_yamls"
  - src: "https://github.com/openstack-k8s-operators/architecture"
    dest: "{{ cifmw_reproducer_controller0_user_dir }}/src/github.com/openstack-k8s-operators/architecture"

# one place to rule them all
cifmw_reproducer_nm_dnsmasq: "/etc/NetworkManager/conf.d/zz-dnsmasq.conf"
cifmw_reproducer_nm_delegation: >-
  /etc/NetworkManager/dnsmasq.d/cifmw-delegate.conf
