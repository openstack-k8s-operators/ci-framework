---
galaxy_info:
  role_name: federation
  namespace: cifmw
  author: CI Framework Team
  description: OpenStack Keystone federation with Keycloak IdP
  company: Red Hat, Inc.
  license: Apache-2.0
  min_ansible_version: "2.9"
  platforms:
    - name: EL
      versions:
        - "8"
        - "9"
  galaxy_tags:
    - openstack
    - keystone
    - federation
    - keycloak
    - sso
    - authentication
    - identity

dependencies:
  - role: ci_setup
    when: cifmw_federation_setup_ci | default(false)

collections:
  - kubernetes.core
  - community.general 