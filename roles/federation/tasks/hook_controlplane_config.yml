---
# Copyright Red Hat, Inc.
# All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.

- name: Set multi-realm mode flag
  ansible.builtin.set_fact:
    cifmw_federation_multi_realm: false

- name: Include shared federation tasks
  ansible.builtin.include_tasks: shared_tasks.yml

- name: Generate OIDC configuration
  ansible.builtin.include_tasks: shared_tasks.yml
  vars:
    task_name: "Generate OIDC configuration for realm"

- name: Generate location match configurations
  ansible.builtin.include_tasks: shared_tasks.yml
  vars:
    task_name: "Generate location match configurations for realms"

- name: Create file to customize keystone for Federation resources deployed in the control plane
  ansible.builtin.copy:
    dest: "{{ cifmw_basedir }}/artifacts/manifests/kustomizations/controlplane/keystone_federation.yaml"
    content: |-
      apiVersion: kustomize.config.k8s.io/v1beta1
      kind: Kustomization
      resources:
      - namespace: {{ cifmw_federation_run_osp_cmd_namespace }}
      patches:
      - target:
          kind: OpenStackControlPlane
          name: .*
        patch: |-
          - op: add
            path: /spec/tls
            value: {}
          - op: add
            path: /spec/tls/caBundleSecretName
            value: keycloakca
          - op: add
            path: /spec/keystone/template/httpdCustomization
            value:
              customConfigSecret: keystone-httpd-override
          - op: add
            path: /spec/keystone/template/customServiceConfig
            value: |
              [DEFAULT]
              insecure_debug={{ cifmw_federation_insecure_debug }}
              debug={{ cifmw_federation_debug_enabled }}
              [federation]
              trusted_dashboard={{ cifmw_federation_horizon_url }}/dashboard/auth/websso/
              [openid]
              remote_id_attribute=HTTP_OIDC_ISS
              [auth]
              methods = password,token,oauth1,mapped,application_credential,openid
    mode: "0644"

- name: Create Keystone httpd override secret for Federation
  kubernetes.core.k8s:
    kubeconfig: "{{ cifmw_openshift_kubeconfig }}"
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: keystone-httpd-override
        namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
      type: Opaque
      stringData:
        federation.conf: |
          {{ oidc_config }}
          {{ location_matches }}
