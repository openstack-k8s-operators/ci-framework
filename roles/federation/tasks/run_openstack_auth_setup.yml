---
# Copyright Red Hat, Inc.
# All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may
# not use this file except in compliance with the License. You may obtain
# a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS, WITHOUT
# WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied. See the
# License for the specific language governing permissions and limitations
# under the License.

- name: Render federation get token script
  ansible.builtin.template:
    src: get-token.sh.j2
    dest: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'get-token.sh' ] | path_join }}"
    mode: '0755'

- name: Copy federation get token script file into pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/get-token.sh"
    local_path: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'get-token.sh' ] | path_join }}"

- name: Render federation test user1 cloudrc template
  ansible.builtin.template:
    src: kctestuser1.j2
    dest: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', cifmw_federation_keycloak_testuser1_username ] | path_join }}"
    mode: "0644"

- name: Render federation test user2 cloudrc template
  ansible.builtin.template:
    src: kctestuser2.j2
    dest: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', cifmw_federation_keycloak_testuser2_username ] | path_join }}"
    mode: "0644"

- name: Copy federation test user1 cloudrc file into pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/{{ cifmw_federation_keycloak_testuser1_username }}"
    local_path: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', cifmw_federation_keycloak_testuser1_username ] | path_join }}"

- name: Copy federation test user2 cloudrc file into pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/{{ cifmw_federation_keycloak_testuser2_username }}"
    local_path: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', cifmw_federation_keycloak_testuser2_username ] | path_join }}"

- name: Copy system CA bundle
  ansible.builtin.copy:
    src: "/etc/pki/ca-trust/extracted/pem/tls-ca-bundle.pem"
    dest: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'full-ca-list.crt' ] | path_join }}"
    mode: "0444"

- name: Get ingress operator CA cert
  ansible.builtin.slurp:
    src: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'ingress-operator-ca.crt'] | path_join }}"
  register: federation_sso_ca

- name: Add ingress operator CA to bundle
  ansible.builtin.blockinfile:
    path: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'full-ca-list.crt' ] | path_join }}"
    block: "{{ federation_sso_ca.content | b64decode }}"

- name: Copy CA bundle to openstackclient pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/full-ca-list.crt"
    local_path: "{{ [ ansible_user_dir, 'ci-framework-data', 'tmp', 'full-ca-list.crt' ] | path_join }}"

# =============================================================================
# OIDC Authentication Method Scripts
# =============================================================================
# These scripts support testing different OIDC authentication methods:
#   - v3oidcclientcredentials: Client Credentials flow
#   - v3oidcaccesstoken: Access Token Reuse flow
#   - v3oidcauthcode: Authorization Code flow

- name: Render OIDC client credentials script
  ansible.builtin.template:
    src: oidc-clientcredentials.sh.j2
    dest: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-clientcredentials.sh'] | path_join }}"
    mode: '0755'

- name: Copy OIDC client credentials script to pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/oidc-clientcredentials.sh"
    local_path: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-clientcredentials.sh'] | path_join }}"

- name: Render OIDC access token script
  ansible.builtin.template:
    src: oidc-accesstoken.sh.j2
    dest: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-accesstoken.sh'] | path_join }}"
    mode: '0755'

- name: Copy OIDC access token script to pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/oidc-accesstoken.sh"
    local_path: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-accesstoken.sh'] | path_join }}"

- name: Render OIDC authorization code script
  ansible.builtin.template:
    src: oidc-authcode.sh.j2
    dest: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-authcode.sh'] | path_join }}"
    mode: '0755'

- name: Copy OIDC authorization code script to pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/oidc-authcode.sh"
    local_path: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'oidc-authcode.sh'] | path_join }}"

- name: Render Keycloak token helper script
  ansible.builtin.template:
    src: get-keycloak-token.sh.j2
    dest: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'get-keycloak-token.sh'] | path_join }}"
    mode: '0755'

- name: Copy Keycloak token helper script to pod
  kubernetes.core.k8s_cp:
    namespace: "{{ cifmw_federation_run_osp_cmd_namespace }}"
    pod: openstackclient
    remote_path: "/home/cloud-admin/get-keycloak-token.sh"
    local_path: "{{ [ansible_user_dir, 'ci-framework-data', 'tmp', 'get-keycloak-token.sh'] | path_join }}"
